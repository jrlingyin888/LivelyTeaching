<h1 align="center">小象课堂 · LivelyTeaching</h1>

<p align="center">点一个图，孩子就能上手做一个真的实验。</p>

<p align="center">
  <img alt="version" src="https://img.shields.io/badge/version-0.2.0-2e6f9e">
  <img alt="three.js" src="https://img.shields.io/badge/three.js-r160-1f3864">
  <img alt="license" src="https://img.shields.io/badge/license-MIT-1f7a5a">
</p>

---

不是播动画，不是看视频——孩子可以拖、可以点、可以猜错重来，**物理是真算的**。

当前版本面向**一年级**：不识几个字也能自己玩完一课。所有关键提示都会念出来，界面上没有小数、没有单位、没有看不懂的仪表盘。

构建产物是**一个 HTML 文件**：双击就能开，不联网、不装环境。

---

## 快速开始

```bash
npm install
npm run build      # 产出 dist/index.html（单文件）
npm run dev        # 开发模式，自动重建
npm run serve      # 本地起服务预览
npm run check      # 课件自检（见下）
```

打开 `dist/index.html` → 点一张图 → 开始。

---

## 已实现的四课

### 1. 认识立体图形 · 一年级数学

长方体 / 正方体 / 圆柱 / 球。五步：认一认 → 看一看 → 滚一滚 → 堆一堆 → 分一分。

3D 在这里有不可替代的价值：一年级最难的一点是**同一个物体从不同角度看形状不一样**。「圆柱从上面看是什么形状？」——先让孩子猜，再把镜头飞到正上方揭晓。

滚坡是真算的：

```
a = g·sinθ / (1 + I/mr²)      球 2/5 · 圆柱 1/2 · 方块不滚
```

球和圆柱的快慢只差 3%，肉眼看不出，所以教学落点放在「能滚 / 不能滚」，**不拿看不见的差别当结论**。

### 2. 谁轻谁重 · 一年级科学

天平。四步：猜一猜 → 大的一定重吗 → 用方块量 → **排一排**。

第二步是专门用来撞前概念的：**大气球 vs 小石头**，多数孩子会猜大的重。天平给出相反的答案。

倾角按真实等臂天平的平衡方程算：

```
tanθ = (mL − mR)·L / (M·d)
```

第三步让孩子一块一块加小方块（气球 2 块 · 苹果 3 块 · 石头 5 块），
加多了天平会往另一边倒——**试错过程是保留的**。

第四步是真的排序：孩子点着把三个物体从重到轻摆好。这三个的**视觉大小顺序和轻重顺序
完全相反**（气球最大最轻、石头最小最重），靠眼睛一定排错，只能照着刚量出来的方块数排——
这就是"用同一样东西去量"这件事的价值。点错不惩罚，只提示"方块多的那个更重"。

### 3. 曹冲称象 · 演示用

面向老师演示和高年级，保留了数值面板。浮力真算：`d = m / (ρ·A)`。

---

### 4. 四季是怎么来的 · 小学科学（玩具模式 · 两根轴）

**没有脚本，没有旁白，没有弹窗。底下两根滑块，拖它们。**

| 滑块 | 拖它看什么 |
|---|---|
| 📅 第几天 | 太阳能爬多高、白天多长、地球走到轨道哪儿 |
| 🕛 几点 | 太阳在天上的走位、影子转到哪边、天什么颜色 |

场上站着一个小人和一根杆，**影子是真投出来的**——一盏平行光摆在太阳的方向上，
Three.js 投的，不是画的。影子长短直接用「几个人」报，一年级不用懂角度也能比。

天上那条弧是**这一天太阳走过的整条路线**。拖「第几天」，整条弧会跟着升降变形：

| | 夏至 | 冬至 |
|---|---|---|
| 正午太阳高 | 73° | **27°** |
| 正午影子 | 0.3 个人 | **2.0 个人** |
| 下午 4 点 | 还很亮，太阳 37° | **太阳 5°，影子 10.4 个人，天在发紫** |
| 白天 | 14.8 小时 | **9.2 小时** |
| 离太阳 | 15203 万公里 | **14716 万公里** |

最后一行是这一课的引爆点：**冬天反而离太阳更近。** 它不是一道被问的题，
是孩子拖着滑块自己撞上去的。

#### 全真算

太阳在天上的位置用地平坐标系三分量给全（e 东 / n 北 / u 天顶）：

```
e = −cos δ · sin H
n =  sin δ · cos φ − cos δ · sin φ · cos H
u =  sin δ · sin φ + cos δ · cos φ · cos H
    δ 太阳赤纬 = 23.44°·sin(2π(N−81)/365)，φ 纬度，H = (时刻−12)×15° 时角

影子长 = 身高 · √(e²+n²) / u          白昼 = 2·arccos(−tanφ·tanδ)/15 小时
日地距离 = 1.496·(1 − 0.0167·cos(2π(N−3)/365)) 亿 km
```

校验过：春分正午 50.0°（= 90−40 ✓）、夏至正午 73.4° ✓、春分早六点太阳正东且高度 0° ✓。

#### 「真实感」是怎么来的

不是贴图，是**天色随太阳高度连续变**——白天蓝、擦黑整片橙红、入夜深蓝出星星。
这一条比任何模型都提气，而且它本身就是被物理驱动的。

其余几样：太阳的光晕是一张径向渐变精灵（比上一整套后期便宜太多）、
2200 颗亮度不一的星、落日时太阳变大变红。

（用不上 Spine —— 那是 2D 骨骼动画，这里是真 3D 场景。）

---

## 面向一年级做了什么

| | |
|---|---|
| **朗读** | 关键提示一律念出来（`speechSynthesis`，全平台）。孩子不识字，但听得懂 |
| **不用数字讲事实** | 天平就是倾角，滚坡就是谁先到底，数量用一排图形表示 |
| **一次最多 3 个大按钮** | 该点哪个就亮哪个 |
| **猜错不惩罚** | 只说「再看看」，不打叉。一年级一旦有挫败感就不肯玩了 |
| **节奏不依赖朗读** | 家长关掉朗读、或浏览器没有 TTS 时，仍按字数留出停顿 |

---

## 课件自检

`npm run check` 跑的是**不需要人看就能判定对错**的规则。每一条都来自一次真实的返工：

- 每节课至少要有一个「先猜再看」的预测点，否则学生只是在看动画
- 每节课必须走得到结论
- 天平的物体质量必须是方块单位的整数倍，否则会量出「五块多一点」
- 关键词不能撞车（撞了会静默匹配错场景）
- 首页卡片需要的字段一个都不能少

人工发现一次，就沉淀成这里的一条，之后不用再靠眼睛盯。

---

## 目录结构

```
src/
  core/
    stage.js        Three.js 舞台：渲染、光照、镜头飞行、dispose、GLB 加载
    ui.js           界面层：朗读大字、预测卡、揭晓、图形化计数、结论卡、首页
    speak.js        语音朗读
  scenes/
    index.js        场景注册表（顺序就是首页卡片顺序）
    solid-shapes.js         认识立体图形
    balance-scale.js        谁轻谁重
    caochong-chengxiang.js  曹冲称象
  main.js           入口：首页 → 装配舞台 → 可随时换一课
  shell.html        HTML 外壳（构建时把 JS 内联进来）
check.mjs           课件自检
build.mjs           单文件构建
```

---

## 教学编排是声明式的

场景切成两半：

```
场景  =  编排（flow · 声明式）  +  内核（build · 手写：建模 + 物理 + 交互循环）
```

编排只有 7 个词，没有表达式语言：

```js
{say: '这是一台天平。哪边沉下去，哪边就更重', hold: 3400}
{ask: '苹果和小积木，你猜哪个重？', as: 'g1', options: [...]}
{act: 'weigh', args: {a: 'apple', b: 'block'}, as: 'heavier1'}
{judge: {expect: 'heavier1', against: 'g1'}, hint: '苹果那边沉下去了'}
```

`expect` 指向一个变量——答案是**算出来的**（`weigh` 真把两个东西放上天平算力矩）；
`correct` 是写死的字面值。自检会提醒一节课里有没有真算的判定。

**交互循环（反复加方块、点着排序）归内核，不进 DSL**，否则 DSL 会长成一门编程语言，
既不好生成也验不了。

步骤条从 `flow` 派生，不用在 meta 里另写一份——两份必然会不同步。

## 用 AI 生成教学编排

因为编排是声明式的，它可以被生成，也可以被机器校验。

```bash
# 1. 组装 prompt（DSL 词汇表 + 这一课内核提供的动作 + 已有场景范例 + 校验规则）
node gen.mjs seasons "四季是怎么形成的？为什么夏天热、冬天冷？给一年级看…"

# 2. 把 prompts/seasons.md 喂给一个干净的模型会话，拿回 JSON

# 3. 过尺子 —— 和手写编排完全同一套规则
node gen.mjs --verify seasons generated/seasons.flow.json

# 4. 装回场景
node gen.mjs --apply seasons generated/seasons.flow.json
```

传输层是可替换的：现在靠人（或子代理）中转，等接了 API 只需在 `gen.mjs` 里加二十行。

**「四季是怎么来的」这一课的编排就是这样生成的**，一次通过全部校验，零修改。
`generated/seasons.handwritten.json` 是同一课的手写版，留着做对照。

## 新增一个场景

新建一个文件 + 在注册表加一行，**不用碰 `src/core/`**。完整规范见 **[docs/场景模板规范.md](docs/场景模板规范.md)**。

---

## 文档

| | |
|---|---|
| [产品文档](docs/产品文档.md) | 定位、用户、设计决策、路线图 |
| [场景模板规范](docs/场景模板规范.md) | 怎么新增一个学科场景 |
| [接入 AI 生成链路](docs/接入AI生成链路.md) | 即梦 → Tripo → GLB，含成本测算 |

---

## 路线图

**第一期（当前）** 自用版。给自家一年级孩子用，看效果，再给几位朋友试。

**下一期** 取决于第一期的真实反馈。可能的方向：

- 按一年级课标补齐更多知识点
- 把两个场景重复的教学编排抽成声明式描述（写完第二个场景之后再抽，不预先设计）
- 更远：AI 生成课件 + 多模型质检流水线，让课件不再靠手写

---

## 技术栈

Three.js · esbuild · Web Speech Synthesis

## License

MIT
